name: 🚀 Деплой Homework Bot (Вход по паролю)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🔨 Build
      run: npm run build
      
    - name: 📦 Create deployment archive
      run: |
        tar -czf homework-bot-deploy.tar.gz \
          dist/ \
          package*.json \
          ecosystem.config.js \
          docker-compose.direct.yml \
          Dockerfile \
          DEPLOY_OPTIONS.md \
          deploy.sh \
          env.example
        echo "📦 Создан архив Docker развертывания: $(ls -lh homework-bot-deploy.tar.gz)"
        
    - name: 📤 Upload archive to server
      uses: appleboy/scp-action@v0.1.7
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        source: "homework-bot-deploy.tar.gz"
        target: "/opt/homework-bot/"
        strip_components: 0
       
    - name: 🖥️ Deploy with Docker
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        password: ${{ secrets.SERVER_PASSWORD }}
        port: ${{ secrets.SERVER_PORT || 22 }}
        script: |
          cd /opt/homework-bot
          
          echo "🔧 Проверяем Docker..."
          if ! command -v docker &> /dev/null; then
            echo "📦 Устанавливаем Docker..."
            curl -fsSL https://get.docker.com | sh
            sudo systemctl enable docker
            sudo systemctl start docker
          fi
          
          if ! command -v docker-compose &> /dev/null; then
            echo "📦 Устанавливаем Docker Compose..."
            sudo apt-get install -y docker-compose
          fi
          
          echo "🛑 Останавливаем текущий контейнер..."
          docker-compose down || true
          
          echo "📦 Извлекаем новый код..."
          if [ -f homework-bot-deploy.tar.gz ]; then
            tar -xzf homework-bot-deploy.tar.gz
            rm homework-bot-deploy.tar.gz
          else
            echo "❌ Архив не найден!"
            exit 1
          fi
          
          echo "📁 Настраиваем окружение..."
          if [ ! -f .env ]; then
            cp env.example .env
            echo "⚠️ Создан .env файл - заполните токены!"
          fi
          
          echo "🔨 Пересобираем Docker образ..."
          cp docker-compose.direct.yml docker-compose.yml
          docker-compose build
          
          echo "🚀 Запускаем контейнер..."
          docker-compose up -d
          
          echo "🧹 Очищаем старые образы..."
          docker image prune -f
          
          echo "✅ Развертывание завершено!"
          echo "📊 Статус контейнера:"
          docker-compose ps
