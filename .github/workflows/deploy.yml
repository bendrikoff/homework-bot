name: 🚀 Деплой Homework Bot

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 🔧 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        
    - name: 📦 Install dependencies
      run: npm ci
      
    - name: 🔨 Build
      run: npm run build
      
    - name: 🔐 Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🏗️ Build Docker image
      run: |
        docker build -t ghcr.io/${{ github.repository }}/homework-bot:latest .
        
    - name: 🚀 Push Docker image
      run: |
        docker push ghcr.io/${{ github.repository }}/homework-bot:latest
        
    - name: 🖥️ Deploy to server
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        script: |
          cd /opt/homework-bot
          
          # Обновляем docker-compose.prod.yml с правильным образом
          sed -i "s|YOUR_DOCKER_USERNAME/homework-bot|ghcr.io/${{ github.repository }}/homework-bot|g" docker-compose.prod.yml
          
          # Копируем prod файл
          cp docker-compose.prod.yml docker-compose.yml
          
          # Авторизуемся в GHCR
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          
          # Перезапускаем контейнер
          docker-compose down || true
          docker-compose pull
          docker-compose up -d
          
          # Очищаем старые образы
          docker image prune -f
          
          # Показываем статус
          docker-compose ps
